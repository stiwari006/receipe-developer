/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Optimize for Vercel deployment
  // Note: 'standalone' output is not needed for Vercel, it handles this automatically
}

module.exports = nextConfig
